# Triggering run to verify permissions - v2
name: Deploy Identity API

on:
  workflow_dispatch: {}
  push:
    branches: [ "main" ]

env:
  DOTNET_VERSION: '9.0.x'
  REGISTRY: ghcr.io
  DB_CONNECTION_STRING: 'mongodb+srv://tommy_db_user:Tommy123456@cluster0.dcuwhnu.mongodb.net/SinglePoint_en?connectTimeoutMS=10000&serverSelectionTimeoutMS=10000&socketTimeoutMS=10000'
  
jobs:
  deploy-dev:
    name: Deploy to Dev
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    environment:
      name: dev
      url: https://pendogo-identity-api-882473783707.asia-southeast1.run.app/swagger/index.html
    permissions:
      contents: read
      id-token: write
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Google Auth
      id: auth
      uses: 'google-github-actions/auth@v2'
      with:
        workload_identity_provider: 'projects/882473783707/locations/global/workloadIdentityPools/github-pool/providers/github-provider-final'
        service_account: 'github-deployer@openclaw-pendogo.iam.gserviceaccount.com'

    - name: 'Set up Cloud SDK'
      uses: 'google-github-actions/setup-gcloud@v2'

    - name: 'Configure Docker'
      run: gcloud auth configure-docker gcr.io --quiet

    - name: 'Build Identity API'
      run: |
        docker build -t gcr.io/openclaw-pendogo/pendogo-identity-api:${{ github.sha }} -t gcr.io/openclaw-pendogo/pendogo-identity-api:latest -f Services/Authentication/CRM.Authentication.Api/Dockerfile .
        docker push gcr.io/openclaw-pendogo/pendogo-identity-api:${{ github.sha }}
        docker push gcr.io/openclaw-pendogo/pendogo-identity-api:latest

    - name: 'Build Customer API'
      run: |
        docker build -t gcr.io/openclaw-pendogo/pendogo-customer-api:${{ github.sha }} -t gcr.io/openclaw-pendogo/pendogo-customer-api:latest -f Services/Customer/CRM.Customer.Api/Dockerfile .
        docker push gcr.io/openclaw-pendogo/pendogo-customer-api:${{ github.sha }}
        docker push gcr.io/openclaw-pendogo/pendogo-customer-api:latest

    - name: 'Build Product API'
      run: |
        docker build -t gcr.io/openclaw-pendogo/pendogo-product-api:${{ github.sha }} -t gcr.io/openclaw-pendogo/pendogo-product-api:latest -f Services/Product/CRM.Product.Api/Dockerfile .
        docker push gcr.io/openclaw-pendogo/pendogo-product-api:${{ github.sha }}
        docker push gcr.io/openclaw-pendogo/pendogo-product-api:latest

    - name: 'Deploy Identity to Cloud Run'
      run: |
        gcloud run deploy pendogo-identity-api \
          --image gcr.io/openclaw-pendogo/pendogo-identity-api:${{ github.sha }} \
          --region asia-southeast1 \
          --platform managed \
          --allow-unauthenticated \
          --port 8080 \
          --min-instances 1 \
          --cpu 1 \
          --memory 512Mi \
          --cpu-boost \
          --timeout 120 \
          --set-env-vars "^|^ASPNETCORE_ENVIRONMENT=Development|JwtSettings__Secret=ThisVerifySecretMustBeLongEnoughForIdentityApiAndNotSharedInPublicRepo1234567890|DatabaseSettings__DBProvider=mongodb|MongoDB__ConnectionString=${{ env.DB_CONNECTION_STRING }}|MongoDB__DatabaseName=SinglePoint_en|CUSTOMER_API_URL=https://pendogo-customer-api-882473783707.asia-southeast1.run.app"

    - name: 'Deploy Customer to Cloud Run'
      run: |
        gcloud run deploy pendogo-customer-api \
          --image gcr.io/openclaw-pendogo/pendogo-customer-api:${{ github.sha }} \
          --region asia-southeast1 \
          --platform managed \
          --allow-unauthenticated \
          --port 8080 \
          --min-instances 1 \
          --cpu 1 \
          --memory 512Mi \
          --cpu-boost \
          --timeout 120 \
          --set-env-vars "^|^ASPNETCORE_ENVIRONMENT=Development|MongoDB__ConnectionString=${{ env.DB_CONNECTION_STRING }}|MongoDB__DatabaseName=SinglePoint_en"

    - name: 'Deploy Product to Cloud Run'
      run: |
        gcloud run deploy pendogo-product-api \
          --image gcr.io/openclaw-pendogo/pendogo-product-api:${{ github.sha }} \
          --region asia-southeast1 \
          --platform managed \
          --allow-unauthenticated \
          --port 8080 \
          --min-instances 1 \
          --cpu 1 \
          --memory 512Mi \
          --cpu-boost \
          --timeout 120 \
          --set-env-vars "^|^ASPNETCORE_ENVIRONMENT=Development|MongoSettings__ConnectionString=${{ env.DB_CONNECTION_STRING }}|MongoSettings__DatabaseName=SinglePoint_en"

    - name: 'Deploy API Gateway'
      run: |
        CONFIG_ID="pendogo-config-${{ github.sha }}"
        gcloud api-gateway api-configs create $CONFIG_ID \
          --api=pendogo-crm-api \
          --openapi-spec=api-gateway.yaml \
          --project=openclaw-pendogo
        
        gcloud api-gateway gateways update pendogo-crm-gateway \
          --api=pendogo-crm-api \
          --api-config=$CONFIG_ID \
          --location=us-central1 \
          --project=openclaw-pendogo

    - name: Notify deployment
      run: |
        echo "🚀 Deployed to GCP Dev"
        echo "URL: https://pendogo-identity-api-882473783707.asia-southeast1.run.app/swagger/index.html"

  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
      packages: write
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Get version from tag
      id: version
      run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Login to Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Build and Push Gateway
      uses: docker/build-push-action@v5
      with:
        context: .
        file: Gateway/CRM.Gateway.Api/Dockerfile
        push: true
        tags: |
          ${{ env.REGISTRY }}/${{ github.repository }}/gateway:${{ steps.version.outputs.VERSION }}
          ${{ env.REGISTRY }}/${{ github.repository }}/gateway:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max

    - name: Build and Push Identity API
      uses: docker/build-push-action@v5
      with:
        context: .
        file: Services/Authentication/CRM.Authentication.Api/Dockerfile
        push: true
        tags: |
          ${{ env.REGISTRY }}/${{ github.repository }}/identity-api:${{ steps.version.outputs.VERSION }}
          ${{ env.REGISTRY }}/${{ github.repository }}/identity-api:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max

    # Create GitHub Release
    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        generate_release_notes: true
        body: |
          ## Docker Images
          - Gateway: `${{ env.REGISTRY }}/${{ github.repository }}/gateway:${{ steps.version.outputs.VERSION }}`
          - Identity API: `${{ env.REGISTRY }}/${{ github.repository }}/identity-api:${{ steps.version.outputs.VERSION }}`
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}


